import { StyleSheet } from "react-native"

export default StyleSheet.create({
    formView: {
        padding: 10
    },
    formRow: {
        flexDirection: 'row',
        justifyContent: 'space-between',
        alignItems: 'center',
        marginVertical: 15
    },
    formRowInput: {
        width: '50%',
        paddingRight: 10
    }
});